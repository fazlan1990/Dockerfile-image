class Agent:
    def __init__(self, Id, Name, EmailAddress, Enabled, Admin, Agent, CustomerId, AllowPortalLogin, JobTitle, Department, LineManager, Office, PhoneNumber, PreferredContactMethod, CustomerName, ChangeManager, CreatedUsingAzureSyncId, Permission_MassComms):
        self.id = Id
        self.name = Name
        # self.emailaddress = EmailAddress
        # self.enabled = Enabled
        # self.agent = Agent
        # self.allowportallogin = AllowPortalLogin